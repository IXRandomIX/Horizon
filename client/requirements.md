## Packages
framer-motion | Page transitions and sleek hover animations

## Notes
The app forces a Vanta Black dark theme.
The frontend automatically replaces {COVER_URL} and {HTML_URL} tokens from the API data to point to the actual CDNs to ensure images and games load correctly.
